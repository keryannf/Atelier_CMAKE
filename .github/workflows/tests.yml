name: Automatisation des tests

on:
  push:

jobs:
  Compilation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Compilation et tests
        run: |
          mkdir build
          cd build
          cmake ..
          make
          make test
